services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_VERSION: "dev"
      PORT: "8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/health"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
